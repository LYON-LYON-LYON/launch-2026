# 2026年航天发射年度总览网站 — 增量演进设计方案

| 文档信息 | 内容 |
|---|---|
| 文档类型 | 设计方案 + 任务分解 + 分步操作说明 |
| 编制人 | 高见远（Gao）· 架构师 |
| 编制日期 | 2026-09-02 |
| 适用版本 | 现网站 v7 → v8+（增量演进，不推翻重写） |
| 读者 | LYON（航天爱好者，非专业开发者，Windows 本地环境） |

---

## 一、现状架构诊断

### 1.1 现状文件

项目当前为两个文件（实际位于 `D:\工作文件夹\全员营销、新业务\卫星通信\火箭发射及卫星入轨统计网站\`，与 PRD 同目录）：

| 文件 | 行数 | 职责 |
|---|---|---|
| `2026年中国航天发射年度总览.html` | 1001 行 | 全部 CSS（内联）+ 全部 JS（内联）+ 页面骨架 |
| `data.js` | 594 行 | 数据层：VENDORS / ROCKETS / OPERATORS / SITES / CATS / EVENTS / MILESTONES + 常量 + 2 个查找函数 |

### 1.2 现状模块清单（基于代码逐段核对）

**data.js（数据层，594 行）**

| 模块 | 职责 | 所在行数 | 健康度 | 说明 |
|---|---|---|---|---|
| VENDORS | 厂家两级分组表（14 火箭 + 11 运营方） | 19-267 | 🟢 良好 | 结构清晰，models 关联型号 |
| ROCKETS | 火箭型号表（41 型） | 270-314 | 🟡 注意 | 302-303 行 `"starship"` key **重复定义**（后者覆盖前者），建议合并 |
| OPERATORS | 卫星运营方表 | 317-342 | 🟡 注意 | `"oneweb"` key 重复定义两次（335、341 行），且与 VENDORS.operator 的 oneweb 存在职责重叠 |
| findRocketKey / findOperatorKey | 名称模糊反查函数 | 348-378 | 🟢 良好 | 目前仅预留，页面主链路未强依赖 |
| SITES / 常量 | 发射场表、YEAR/CATS/ST_ICON/ST_TXT/MNAME | 381-404 | 🟢 良好 | CATS 8 类与图例一致 |
| EVENTS | 全年 124 项事件 | 406-571 | 🟢 良好 | 字段规范见注释头；无 source 来源字段（**追溯性缺口**） |
| MILESTONES | 大事记 18 条 | 574-594 | 🟢 良好 | 纯人工维护，符合 PRD 约定 |

**主 HTML（渲染层 + 交互层，1001 行）**

| 模块 | 职责 | 所在行数 | 健康度 | 说明 |
|---|---|---|---|---|
| CSS | 全部样式（含 color-mix 现代特性） | 7-320 | 🟢 良好 | 变量化好，需 Chrome/Edge 111+ |
| 页面骨架 | header / tabs / 首页 / 月份视图 / 专属页 / 图例 / footer | 322-412 | 🟢 良好 | 静态骨架 + JS 填充 |
| 状态与工具 | 筛选状态、RK2VENDOR/OP2VENDOR 反查表、日期工具 | 416-448 | 🟢 良好 | v7 反查表设计合理 |
| renderStats | 顶部统计条（三重过滤联动） | 451-467 | 🟢 良好 | v7 交付 |
| renderVendorRow | 厂家入口两级分组渲染 | 470-518 | 🟢 良好 | 零发射隐藏逻辑在此 |
| renderTabs / 视图切换 | 月份页签、地区三档切换 | 521-539 | 🟢 良好 | |
| renderHome / renderMilestones | 首页月卡 + 大事记时间轴 | 542-585 | 🟢 良好 | |
| 月份视图（showMonth / renderFilters / renderCalendar / renderPanel） | 日历核心 + 图例/类型筛选 + 全选按钮 | 588-753 | 🟢 良好 | 核心资产，逻辑最重 |
| cardHTML | 事件卡片（火箭/运营方字段跳公司主页） | 695-723 | 🟢 良好 | 是搜索功能可复用的渲染单元 |
| showDetail | 型号/运营方详情页（含公司双向跳转） | 756-824 | 🟢 良好 | |
| showVendorDetail | 公司主页（档案/统计/月度节奏/型号卡/入轨构成/记录） | 827-985 | 🟢 良好 | v6 核心，约 160 行最长函数 |
| 启动链 | renderStats → renderVendorRow → renderTabs → renderHome → showMonth | 997-998 | 🟡 脆弱点 | 无错误兜底，任何一个函数抛错整站白屏（v5.1 教训） |

### 1.3 单文件架构的天花板与风险（结合 PRD 第 8 节）

| # | 风险 | 说明 |
|---|---|---|
| 1 | **无版本管理** | 当前文件散落在工作目录，没有任何历史版本快照。误改坏一个函数（如 v5.1）只能靠记忆回滚。这是当前**最紧迫**的风险 |
| 2 | **手工数据滞后** | data.js 全靠人工维护，国际事件是"整理值非逐发核对"，10-12 月全是占位。数据时效是产品核心承诺（新闻及数据有依据、可追溯），纯手工不可持续 |
| 3 | **无公网入口** | 只能本地双击打开，"教学/内容创作者引用截图、大众科普"的用户群无法触达 |
| 4 | **无追溯字段** | EVENTS 无 source 字段，"数据可追溯"验收项目前靠 note 文字描述，不结构化、不可校验 |
| 5 | **启动链无兜底** | 任何一个渲染函数抛错整站白屏，而每次数据改动都可能触发（v5.1 已发生一次） |
| 6 | **单文件体量** | 1001 行 HTML 尚可维护，但后续加搜索、年份切换、火箭规格增强后预计 1300+ 行，非开发者维护难度上升 |
| 7 | **字段级小缺陷** | ROCKETS/OPERATORS 各有一个重复 key（starship/oneweb），JS 对象字面量静默覆盖，不报错但埋雷 |

**结论**：v7 的功能与代码质量本身是健康的，问题不在"写得不好"，而在"缺乏工程化外壳"——没有版本管理、没有自动化数据管道、没有公网部署。演进方向是**给现有的好核心补基础设施**，而非重构。

---

## 二、总体设计方案

### 2.1 技术选型决策：继续「零构建纯静态」

**推荐：维持零构建纯静态，仅引入 GitHub 平台能力（Pages + Actions），本地不装任何开发工具。**

| 候选 | 结论 | 理由 |
|---|---|---|
| 零构建纯静态（现状）+ GitHub | ✅ **推荐** | ① 双击可用性 100% 保留（HTML+data.js 相对引用不变）；② 不要求 Node 经验——Actions 在云端跑，本地零依赖；③ 免费额度完全够用；④ LYON 的维护动作 = 网页上点按钮 + 改 data.js 文本 |
| Vite/Astro 等轻构建 | ❌ 否决 | 需要本地 Node 环境 + npm + 命令行心智模型，"双击可用"变成"先 build 再双击"，违背用户约束；且现有 1000 行代码无组件化诉求 |
| JSON 数据 + fetch | ❌ 否决（维持 data.js） | file:// 协议下 fetch 跨域受限，双击打开会挂；data.js 的 `<script src>` 是唯一双击可用的数据注入方式 |

关键判断：**"数据自动更新"不等于"需要构建工具"**。GitHub Actions 在云端用 Node 抓 API → 直接生成新的 data.js → commit 回仓库 → Pages 自动重新发布。整条链路对本地零要求。

### 2.2 目标架构（文字版分层）

```
┌─────────────────────────────────────────────────────────┐
│ 部署层：GitHub Pages（公网访问） ＋ 本地双击（离线可用）      │
│   同一份静态文件，两种访问方式，零差异                        │
├─────────────────────────────────────────────────────────┤
│ 交互层：app 内 JS（日历渲染/筛选/跳转/搜索）                  │
│   index.html 内联脚本（现状保留）+ 新增 search.js（可选）    │
├─────────────────────────────────────────────────────────┤
│ 渲染层：CSS（内联）+ 视图函数（render* / show*，现状保留）    │
├─────────────────────────────────────────────────────────┤
│ 数据层：data.js（EVENTS/ROCKETS/…，新增 source 字段）       │
│   ↑ 由 scripts/update-data.js（云端）自动重写国际事件        │
│   ↑ 中国事件由 LYON 手工补录（overrides 机制保护）           │
├─────────────────────────────────────────────────────────┤
│ 采集层（仅云端）：GitHub Actions 定时任务                    │
│   Launch Library 2 API（主源）→ 交叉校验 → 写 data.js       │
│   .github/workflows/update-data.yml + scripts/*.js        │
├─────────────────────────────────────────────────────────┤
│ 版本层：Git 仓库（全部历史可回滚，数据每次更新一个 commit）    │
└─────────────────────────────────────────────────────────┘
```

### 2.3 文件结构规划（向后兼容）

```
spaceflight-2026/                    ← 新 git 仓库根
├── index.html                       ← 现主页面改名（双击可用，内引 ./data.js）
├── data.js                          ← 现数据层（新增 source 字段）
├── docs/
│   ├── PRD-2026年航天发射年度总览网站.md
│   ├── 数据字典.md                  ← 新增：字段定义 + 口径约定 + 录入规范
│   └── 维护手册.md                  ← 新增：日常更新操作说明（浓缩自本文档第五节）
├── scripts/
│   ├── fetch-launch-library.js      ← 新增：抓取 LL2 API（云端 Node 运行）
│   ├── build-data.js                ← 新增：LL 数据 + overrides 合并 → 重写 data.js
│   ├── validate-data.js             ← 新增：字段完整性/重复key/日期合法性校验（防 v5.1 类事故）
│   └── overrides/                   ← 新增：人工数据保护区（中国细节/发射批次描述）
│       ├── events-cn-overrides.js   ←   中国事件的批次号、载荷描述等手工字段
│       └── milestones.js             ←   大事记（纯人工，脚本不碰）
├── .github/workflows/
│   ├── update-data.yml              ← 新增：定时抓取（每日 UTC 01:00 = 北京 09:00）
│   └── deploy-pages.yml             ← 新增：push 时发布（也可用 Pages 自带 CI）
└── README.md                        ← 新增：项目一句话说明 + 快速上手
```

兼容性说明：
- `index.html` 内 `<script src="data.js">` 是相对路径，改名换目录后双击照样可用；
- 原 `outputs/` 目录下的两个文件**原样不动**作为本地工作副本，仓库中是归位后的副本（阶段 0 完成复制，之后仓库为主）；
- data.js 顶部的数据结构（EVENTS 等 const 声明）保持不变，脚本用"生成整个文件"而非"打补丁"的方式更新，永远产出完整可读的 data.js。

---

## 三、PRD 需求 → 任务映射

### B1 数据自动更新（高优先级）

**方案要点：GitHub Actions 定时抓取，"自动生成 + 人工保护区"双层结构。**

- **主源**：Launch Library 2 API（`https://ll.thespacedevs.com/2.2.0/launch/`，免费、无 key、有 CORS）。按 `net` 参数分页拉取 2026 年度窗口的国际发射。
- **校验源**：对抓回的关键字段（日期、状态、载荷数）与 Wikipedia 年度发射条目比对；LL2 本身有 slug 稳定标识，同一天同火箭即视为同一任务。
- **合并策略**：
  - 国际事件（SpaceX/Kuiper/OneWeb/其他国际）：以 LL2 为准**自动重写**，现状 49 条"整理值"逐步被真实数据替换；
  - 中国事件：LL2 对中国事件覆盖不全（详见第六章风险），**只做新增提醒不做自动改写**——Actions 输出一份 `data-changes-report.md`（本次新增/变更清单），LYON 核实后手工录入或把字段写进 overrides；
  - 大事记：纯人工，脚本不碰；
  - 卫星数（satCount）：LL2 无直接字段，按载荷名称模式估算（如 "Starlink Group 12-xx" → 22）写入，标记 `source:"ll2-auto"`，人工核对后可改写。
- **产出形态**：每次运行若有数据变化，Actions 自动 commit `data.js`（commit message 带变更统计），Pages 随之自动更新。

### B2 公网部署（高优先级）

**方案要点：GitHub Pages，从仓库 main 分支直接发布。**

- 仓库根即站点根，`index.html` 即首页，无需任何构建步骤；
- Pages 免费额度：软限 100GB/月流量、单次 build 10 分钟，远超本项目量级；
- 网络访问问题见第六章风险对策；
- 部署后双击本地文件的习惯完全不受影响（本地文件与线上是同一份的两个副本，本地改完 push 即同步）。

### B3 10-12 月真实数据回填（高优先级）

**方案要点：口径区分"占位 → 实况"的两态转化，靠 B1 的管道 + 少量人工。**

- 现状：10-12 月中国 4 项 + 国际 9-12 月预估批次，`st:"plan"` / `tbd:1`；
- B1 上线后：国际（星链/Kuiper/OneWeb）的 10-12 月计划会被 LL2 的 `scheduled` 状态任务自动替换为真实排期；
- 中国任务：官方公告后 LYON 手工更新 `s/e/st`，并补 `source`（如"新华社 2026-10-02 通稿"）；
- 验收：12 月底全年 124+ 项事件全部为 done/fail/delay 实况，无 plan 占位残留。

### B4 国际数据扩展（中优先级）

**方案要点：零代码改动，数据驱动自动浮现。**

- ULA/ArianeSpace/俄航局/JAXA/ISRO/Rocket Lab/ABL 的厂家主页（VENDORS 表）已存在，只是无 2026 事件数据；
- B1 抓取范围天然覆盖全球，LL2 返回的 ULA/Ariane 6/Soyuz/H3/GSLV 事件会自动落入 EVENTS → 厂家入口的"零发射隐藏"逻辑自动放行显示；
- 需要做的仅是：`build-data.js` 里维护一张 LL2 rocket name → rkKey 的映射表（约 15 行），新火箭型号出现时补一行映射；
- 注意：厂家入口 chip 会从当前约 10 个增至 15+，届时如觉得乱，可在国际组内再分"美国/欧俄/亚太"三小组（纯渲染改动，1 个函数）。

### B5 卫星在轨总数动态追踪（中优先级）

**方案要点：`inOrbitBase` 基线值 + 年内累加，前端实时计算。**

- data.js 的 OPERATORS 增加 `inOrbitBase`（数字型年初基线，如 starlink: 7700、yuanxin: 220），保留现有 `inOrbit` 字段做兼容展示；
- 展示逻辑：`当前在轨 = inOrbitBase + EVENTS 中该运营方 st==="done" 的 satCount 累加`——前端一行 reduce 即得，天然随数据更新而动，无需后台计算；
- "入轨卫星构成"图表（v6 已有）不变，公司主页档案区的"在轨"数字改为动态值并标注"（截至 YYYY-MM-DD，含 2026 年入轨 N 颗）"；
- 需注意离轨/失效卫星无法从本数据集推导，页脚加口径注释："在轨数 = 年初基线 + 本年度累计入轨，未扣减离轨"。

### B6 搜索功能（低优先级）

**方案要点：纯前端即时搜索，复用 cardHTML 渲染。**

- 顶部加搜索框（HTML 一个 `<input>` + 一个新函数 `renderSearch(q)`）；
- 检索范围：`name / rk / pl / op / site` 字段的不区分大小写包含匹配；命中高亮；
- 结果用现有 `cardHTML()` 渲染成网格（卡片点击行为与日历侧栏一致：选中该日期/跳转公司页）；
- 不引入任何搜索库——124 条数据 `Array.filter` 微秒级完成；
- 交互细节：输入即搜（300ms 防抖）、Esc 清空、结果为空给"未找到含 XX 的任务"提示。

### B7 年份切换（低优先级）

**方案要点：架构已支持，只需数据与入口，不急做。**

- YEAR 常量 + 相对路径 data.js 已天然支持多年份；2027 年复制 `data.js` 为 `data-2027.js` + 主页面读 `?year=2027` 参数或加年份下拉；
- PRD 定义为低优先级，建议 2026 年 12 月数据收官后再启动，避免当前维护两份数据翻倍工作量。

### 新增需求点 1：火箭规格展示增强（对应"看细节"新增维度）

**方案要点：扩展 ROCKETS 字段 + 型号页规格表格。**

- ROCKETS 每型增补结构化字段：`height`（全箭高度 m）、`diameter`（直径 m）、`stages`（级数）、`thrust`（起飞推力 kN）、`maiden`（首飞年份）、`reuse`（是否可复用 0/1）；
- 现有 `leo/geo/fuel/type/note` 保留不变（向后兼容）；
- 型号详情页（showDetail）的 hero 区下方新增"火箭规格卡"：以两列表格展示全部参数，数据缺失的字段显示"—"；
- 事件卡片 hover 提示（title 属性）追加"高度 X m · 运力 Y t"一行，让日历视图也能"看细节"；
- 数据来源：厂家官网/Wikipedia 一次性人工录入（41 型 × 7 字段，约 1-2 小时工作量），录入规范写入数据字典。

### 新增需求点 2：数据可追溯性（对应"新闻及数据有依据、可追溯"验收项）

**方案要点：EVENTS 增加 source 字段 + 页面呈现"来源"链接 + 数据字典固化口径。**

- EVENTS 每条增加 `source` 字段，格式约定：
  - `"source": {"ref": "ll2", "url": "https://ll.thespacedevs.com/...", "checked": "2026-09-02"}`（自动抓取）
  - `"source": {"ref": "新华社", "url": "https://...通稿链接", "checked": "2026-09-02"}`（人工核实）
  - `"source": {"ref": "整理值·待核实", "url": "", "checked": "2026-09-01"}`（现有 49 条国际整理数据的初始状态）
- 事件卡片 note 上方加一行小字"来源：ref ↗"（有 url 则可点击），自动抓取的显示"Launch Library·自动"、人工的显示来源名；
- `validate-data.js` 强制校验：`st` 为 done/fail 的事件必须有非"待核实"的 source（把追溯性变成机器可检的硬约束）；
- 数据字典文档明确定义：每个字段的含义、取值域、录入示例、口径约定（PRD 3.1 的统计口径全部收录）。

---

## 四、任务分解（含依赖关系）

### 4.1 有序任务列表

| ID | 任务名称 | 前置依赖 | 产出物 | 验收标准 |
|---|---|---|---|---|
| T1 | 仓库初始化与文件归位 | 无 | git 仓库 + README + 2.3 节目录结构 | `git log` 有首个 commit；双击 index.html 正常打开（v7 功能无回归） |
| T2 | 修复重复 key（starship/oneweb） | T1 | data.js 修正 | 脚本校验无重复 key；型号页/运营方页渲染正常 |
| T3 | 数据可追溯性改造 | T2 | data.js 全量 source 字段 + docs/数据字典.md | 每条事件有 source；卡片显示"来源"；数据字典覆盖全部字段 |
| T4 | 校验脚本（防白屏护栏） | T2 | scripts/validate-data.js | node 运行通过 = 字段完整/无重复 key/日期合法/状态枚举正确 |
| T5 | GitHub 仓库建立与 Pages 部署 | T1 | 公网 URL | 浏览器访问线上地址功能正常；手机可看（响应式已有） |
| T6 | 抓取脚本 | T4 | scripts/fetch-launch-library.js + build-data.js | 手动触发能产出新 data.js 且校验通过、现有手工数据未被破坏 |
| T7 | Actions 定时任务上线 | T5, T6 | update-data.yml | Actions 页面看到绿色运行记录；有数据变化时自动 commit |
| T8 | 人工保护区与变更报告 | T7 | overrides 机制 + data-changes-report | 中国事件不受自动重写影响；报告列出新增/变更清单 |
| T9 | 10-12 月数据回填 | T7, T8 | 更新后的 data.js | 无 plan 占位残留实况缺失；国际数据与 LL2 一致 |
| T10 | 国际数据扩展 | T7 | LL2→rkKey 映射表 + 新事件 | ULA/Ariane 等有发射后自动出现在厂家入口 |
| T11 | 火箭规格增强 | T3 | ROCKETS 新字段 + 型号页规格卡 | 41 型参数齐全；日历 hover 显示高度/运力 |
| T12 | 在轨总数动态计算 | T3 | inOrbitBase + 前端动态展示 | 星链/千帆在轨数随批次累加；口径注释就位 |
| T13 | 搜索功能 | T3 | 搜索框 + renderSearch | 任务名/火箭/公司检索可用；结果卡片可跳转 |
| T14 | 年份切换（2027 预留） | 延后 | — | 2026 收官后另立方案 |

### 4.2 批次划分

| 批次 | 任务 | 目标 | 说明 |
|---|---|---|---|
| **批次一（地基周）** | T1 → T2 → T3 → T4 | 版本管理 + 数据可信 | 全部本地完成，不碰云；T3/T4 可并行 |
| **批次二（上线周）** | T5 →（T6 并行开发）→ T7 | 公网可访问 + 自动管道 | T5 只依赖 T1，可与 T3/T4 并行；T6 是唯一需要写"真代码"的任务 |
| **批次三（数据周）** | T8 → T9 → T10 | 数据全面回实 | 依赖管道稳定运行；主要是核实工作而非开发 |
| **批次四（体验月）** | T11、T12、T13 | 功能增强 | 三者互相独立可任意顺序/并行；每个独立上线验证 |

节奏建议：批次一、二合计约一周（每天 1-2 小时）；批次三在 10 月官方计划密集公布后启动收益最大；批次四穿插在等待数据的空档期做。

---

## 五、分步骤操作说明（用户操作手册视角）

> 通用约定：以下"终端"指 Windows 开始菜单搜 "PowerShell" 打开的蓝色窗口；每步做完都有"怎么验证"小节，照做即知对错。

### 阶段 0：仓库初始化（对应 T1、T2）

**做什么：把两个文件变成一个 git 管理的项目，并修复两个重复 key。**

**步骤 0.1 安装 Git（如已有跳过）**
1. 浏览器打开 `https://git-scm.com/download/win`，下载并双击安装，一路点 Next（全部默认值）。
2. 验证：开始菜单搜 "Git Bash" 打开，输入 `git --version`，显示 `git version 2.x.x` 即成功。

**步骤 0.2 创建项目目录并归位文件**
1. 打开 PowerShell，逐行执行：
   ```powershell
   mkdir D:\工作文件夹\全员营销、新业务\卫星通信\spaceflight-2026
   cd D:\工作文件夹\全员营销、新业务\卫星通信\spaceflight-2026
   mkdir docs, scripts\overrides, .github\workflows
   Copy-Item "D:\工作文件夹\全员营销、新业务\卫星通信\火箭发射及卫星入轨统计网站\2026年中国航天发射年度总览.html" .\index.html
   Copy-Item "D:\工作文件夹\全员营销、新业务\卫星通信\火箭发射及卫星入轨统计网站\data.js" .\data.js
   Copy-Item "D:\工作文件夹\全员营销、新业务\卫星通信\火箭发射及卫星入轨统计网站\PRD-2026年航天发射年度总览网站.md" .\docs\
   ```
2. 验证：资源管理器打开该目录，能看到 `index.html`、`data.js`、`docs`、`scripts`、`.github` 五项；**双击 index.html** 浏览器正常显示 v7 全部功能（顶部统计有数字、点 8 月出日历、点蓝箭出公司主页）。

**步骤 0.3 修复重复 key**
1. 用记事本（或 VS Code）打开 `data.js`：
   - 找到约 302-303 行两行 `"starship"`，删掉第二行（保留信息更全的那行，把"火星运输"等 note 合并到保留行）；
   - 找到约 335 与 341 行两处 `"oneweb"`，保留第一处（648 颗完成），删除第二处。
2. 验证：页面刷新后，点厂家入口"OneWeb"运营方主页正常显示"648颗(完成)"，无报错（F12 打开控制台无红色错误）。

**步骤 0.4 git 初始化与首个提交**
1. 在该目录下 PowerShell 执行：
   ```powershell
   git init
   git add .
   git commit -m "v7 基线：现有网站两文件归位"
   ```
   首次 commit 若提示设置身份，执行：
   ```powershell
   git config --global user.name "LYON"
   git config --global user.email "你的邮箱@example.com"
   ```
   然后重新 `git commit`。
2. 验证：`git log --oneline` 显示一条提交记录；此时任何误改都可以用 `git checkout -- 文件名` 一键还原——**这就是版本管理的意义**。

### 阶段 1：数据可追溯性改造（对应 T3、T4）

**做什么：给 124 条事件补 source 字段，写数据字典，加校验脚本护栏。**

**步骤 1.1 补录 source 字段（分批做，每次 20 条）**
1. 打开 `data.js`，在 EVENTS 每条对象的 `note` 之前插入 `source` 字段。三类模板：
   - 已核实的中国事件：`source:{ref:"中国载人航天工程办公室", url:"通稿链接", checked:"2026-09-02"},`
   - 国际整理值（现状 49 条）：`source:{ref:"整理值·待核实", url:"", checked:"2026-09-01"},`
   - 权威新闻：`source:{ref:"新华社", url:"...", checked:"2026-09-02"},`
2. 建议 3 个晚上分批完成（1-8 月中国 75 条优先补真实来源；国际 49 条先标"整理值"等阶段 3 自动替换）。
3. 验证：双击 index.html，随便点开一张事件卡片，note 区上方出现"来源：xxx"小字。

**步骤 1.2 修改 cardHTML 显示来源（改 1 处 HTML 代码）**
1. 打开 `index.html`，定位 `cardHTML` 函数里 `${ev.note?...}` 那一行（约 721 行），在其前插入：
   ```js
   ${ev.source?`<div class="note" style="border:none;padding-top:0">来源：${ev.source.url?`<a href="${esc(ev.source.url)}" target="_blank" style="color:var(--blue)">${esc(ev.source.ref)} ↗</a>`:esc(ev.source.ref)}（核实于 ${ev.source.checked||"—"}）</div>`:""}
   ```
2. 验证：刷新页面，卡片显示来源；有 url 的可点击跳转（新标签页打开）。

**步骤 1.3 编写数据字典**
1. 新建 `docs/数据字典.md`，内容包括：EVENTS 全字段定义（含 source 格式）、ROCKETS/OPERATORS/VENDORS 字段、统计口径约定（从 PRD 3.1 抄录：仅计成功入轨/三轨定义/待定不入格/零发射隐藏）、录入示例一条完整事件。
2. 验证：请一位不了解项目的朋友照字典录一条测试事件能录对（或自己隔一周后回看仍能看懂）。

**步骤 1.4 校验脚本（护栏，防 v5.1 类白屏）**
1. 新建 `scripts/validate-data.js`（约 60 行）：读取 data.js 文本 → 正则/eval 提取对象 → 校验：EVENTS 无重复 id、rkKey 在 ROCKETS 存在（或为空）、opKey 同理、cat 在 CATS 枚举、st 在 4 态枚举、s<e、done/fail 事件必须有 source。
2. 运行方式（无需装 Node——本脚本仅设计为在云端 Actions 跑；本地想跑可装 Node LTS，但非必须）：
   - 本地有 Node：`node scripts/validate-data.js` 输出 "ALL OK"；
   - 本地无 Node：留到阶段 3 由 Actions 自动跑，每次数据更新自动把关。
3. 验证：故意把一条事件的 `st` 改成 `"don"` 保存，再跑校验应报错指出行号；改回后恢复 ALL OK。

### 阶段 2：GitHub 仓库建立与 Pages 部署（对应 T5）

**做什么：把本地仓库推上 GitHub，开 Pages，拿到公网地址。**（全程浏览器操作）

**步骤 2.1 注册 GitHub 账号（已有跳过）**
1. 浏览器开 `https://github.com` → 点 Sign up → 按提示注册（用户名建议起一个好记的，会出现在网址里）。
2. 验证：登录后右上角显示你的头像。

**步骤 2.2 新建仓库**
1. 点右上角 **"+" → New repository**。
2. Repository name 填 `spaceflight-2026`；描述可填"2026 全球航天发射年度总览"。
3. 可见性选 **Public**（Public 才能免费用 Pages；介意公开可后续换 Pro，先 Public 起步）。
4. **其余全部不勾选**（不要 README、不要 .gitignore——我们本地已有内容，避免冲突）。
5. 点绿色 **Create repository** 按钮。
6. 验证：出现一个页面写着 "…or push an existing repository from the command line"，说明建仓成功。

**步骤 2.3 推送本地代码**
1. 回到本地 PowerShell（cd 到项目目录），执行（把 `你的用户名` 换成自己的）：
   ```powershell
   git remote add origin https://github.com/你的用户名/spaceflight-2026.git
   git branch -M main
   git push -u origin main
   ```
2. 弹出登录窗口 → 选 **Sign in with your browser** → 授权。
3. 验证：刷新 GitHub 仓库页面，能看到 index.html / data.js / docs / scripts 全部文件。

**步骤 2.4 开启 Pages**
1. 仓库页面点顶部 **Settings**（设置）。
2. 左侧菜单找 **Pages**（在 Code and automation 分组下）。
3. **Source** 下拉选 **Deploy from a branch**；Branch 选 **main**，文件夹选 **/ (root)**，点 **Save**。
4. 等待 1-2 分钟，页面顶部出现绿色提示和地址，形如：
   `https://你的用户名.github.io/spaceflight-2026/`
5. 验证：浏览器（或手机）打开该地址，网站完整显示、日历可点、公司主页可进；再把地址发给朋友测试能否访问。

**步骤 2.5 之后的日常同步**（记住这三行即可）
```powershell
git add .
git commit -m "描述这次改了什么"
git push
```
push 后约 1 分钟 Pages 自动更新——**这就是以后每次数据修改的发布方式**。

### 阶段 3：数据自动更新流水线（对应 T6、T7、T8）

**做什么：让 GitHub 每天早上自动抓国际发射数据、更新 data.js、自动发布。**（本阶段含 3 个脚本文件 + 1 个 YAML，是全项目唯一需要"写代码"的环节，建议由 AI 助手按以下规格生成后 LYON 验收）

**步骤 3.1 抓取脚本 `scripts/fetch-launch-library.js`**
- 功能：Node 18 内置 fetch 调 `https://ll.thespacedevs.com/2.2.0/launch/?format=json&net__gte=2026-01-01&net__lte=2026-12-31&limit=100&offset=N`，分页拉完；
- 过滤：只保留 lsp（launch service provider）或 rocket 命中映射表的国际条目（SpaceX/ULA/ArianeSpace/Roscosmos/JAXA/ISRO/Rocket Lab/ABL）；
- 映射：LL2 字段 → 本项目字段（`net`→s/e、`status.abbrev`→st：Success→done/Failure→fail/In Flight→done/Scheduled→plan、`rocket.configuration`→rkKey 查映射表、`launch_service_provider`→op）；
- satCount 估算：载荷名含 "Starlink Group" 按批次查表（12-xx→22），含 "Kuiper"→27，含 "OneWeb"→36，否则留 0 待人工；
- 限流保护：LL2 匿名限 15 请求/小时，脚本内每次请求间隔 250ms，失败重试 2 次后退避 60s；

**步骤 3.2 合并脚本 `scripts/build-data.js`**
- 读入：LL 抓取结果 JSON + `scripts/overrides/events-cn-overrides.js`（中国事件全量手工数据）+ `scripts/overrides/milestones.js`（大事记）+ 现有 data.js 中的 VENDORS/ROCKETS/OPERATORS/CATS（静态表不自动改）；
- 合并规则：
  1. 中国事件（`ty:"国发"/"商发"`）：完全以 overrides 为准，LL2 数据仅输出到变更报告供参考；
  2. 国际事件（`ty:"国外"`）：以 LL2 为准生成，但**逐条检查 overrides 中是否有同 id 的人工锁定**（`locked:1` 字段），锁定者不覆盖；
  3. `source` 字段自动写 `{ref:"Launch Library", url:LL2 详情页链接, checked:当天}`；
- 产出：全新完整 `data.js`（保持现有人类可读的缩进格式）+ `data-changes-report.md`（新增 N 条/变更 M 条/疑义 K 条清单）；
- 最后调用 `validate-data.js`，校验不过则 exit 1（Actions 显示红叉，不发布坏数据）。

**步骤 3.3 工作流 `.github/workflows/update-data.yml`**（YAML 骨架，Actions 会自动跑）
```yaml
name: 每日数据更新
on:
  schedule:
    - cron: "0 1 * * *"   # UTC 01:00 = 北京时间 09:00
  workflow_dispatch:       # 保留网页手动触发按钮
permissions:
  contents: write
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: "20" }
      - run: node scripts/fetch-launch-library.js   # 抓取
      - run: node scripts/build-data.js             # 合并+校验，产出 data.js 与报告
      - run: |
          if git diff --quiet data.js; then
            echo "无变化，跳过提交"
          else
            git config user.name "data-bot"
            git config user.email "actions@github.com"
            git add data.js data-changes-report.md
            git commit -m "数据自动更新 $(date +%F)"
            git push
          fi
```
1. 部署方式：把这个文件放进 `.github/workflows/` 目录，git push 上去即可。
2. 验证：GitHub 仓库页点 **Actions** 标签 → 左侧看到"每日数据更新"→ 点 **Run workflow** 按钮手动跑一次 → 等待绿色 ✓ → 若有数据变化，仓库首页 data.js 的提交时间更新为刚才，且打开线上网址看到新数据。

**步骤 3.4 人工校正流程（LYON 的日常，5 分钟/天）**
1. 每天上午打开 GitHub 仓库 → Actions → 看最新一次运行是绿是红；红的话点进去看日志（多数是限流，第二天自愈）。
2. 打开 `data-changes-report.md` 看今天新增/变更了什么（特别是"中国事件疑义清单"部分）。
3. 需要人工补细节（批次描述、载荷中文名、核实 satCount）时：本地改 `overrides/events-cn-overrides.js` → git push → 下次运行生效。
4. 每周一次把重要节点手工补进 `overrides/milestones.js`（大事记纯人工）。

### 阶段 4：功能增强（对应 T11、T12、T13）

**做什么：三个独立小功能，每个都基于现有代码小步增量，做完一个验证一个。**

**步骤 4.1 火箭规格增强（T11）**
1. data.js 的 ROCKETS 每型追加 7 字段：`height/diameter/stages/thrust/maiden/reuse/`（例：`"falcon9":{..., height:"70m", diameter:"3.7m", stages:2, thrust:"7600kN", maiden:"2010", reuse:1}`）；数据从厂家官网/Wikipedia 逐型录入，约 41 次。
2. index.html 的 `showDetail` 函数 rocket 分支，在 hero 后插入规格表（复制 heroMeta 的写法，加两行循环渲染新字段）。
3. 事件卡片 title 提示追加一行（cardHTML 里 `seg.title=` 或 `.kv` 区追加）。
4. 验证：点"长征五号"等型号页出现规格卡；日历 hover 任务条显示高度/运力。

**步骤 4.2 在轨总数动态计算（T12）**
1. OPERATORS 各条加 `inOrbitBase` 数字（星网 0、垣信 220、天启 37、starlink 7700、kuiper 0、oneweb 612——以 2025-12-31 为基线，查各官网核一次）。
2. `showVendorDetail` 运营方分支的 metaRows 里"在轨"改为动态：`v.inOrbitBase + 全年该运营方 done 事件 satCount 之和`，并附"（含 2026 年入轨 N 颗，截至 最新事件日期）"。
3. 页脚口径注释加一句"在轨数=年初基线+本年累计入轨，未扣减离轨/失效"。
4. 验证：千帆在轨数应显示 220+238≈458 量级（与 8 月新闻口径一致）；新一次组网数据录入后数字自动 +18。

**步骤 4.3 搜索功能（T13）**
1. header 区 `<div class="stats">` 上方插入：
   ```html
   <input id="searchBox" placeholder="搜索：任务名 / 火箭 / 公司 / 发射场…" style="...">
   ```
2. 新增 `renderSearch(q)` 函数：`EVENTS.filter(五字段包含匹配)` → `cardHTML` 渲染到结果容器；输入事件监听 + 300ms 防抖；Esc 清空返回原视图。
3. 验证：搜"千帆"列出全年所有千帆任务；搜"Falcon"列出星链批次；搜不存在词给空态提示；结果卡片点击跳公司主页正常。

---

## 六、风险与对策

| # | 风险 | 概率/影响 | 对策 |
|---|---|---|---|
| 1 | **LL API 被限流**（匿名 15 次/小时，Actions 云端 IP 是共享池更容易触发） | 中/中 | ① 脚本分页控制在 15 请求内（limit=100 一页够 2026 年国际任务量）；② 请求间隔 250ms + 失败退避；③ cron 每天一次而非每小时；④ 若持续限流：免费注册 thespacedevs 账号申请个人 API key（免费档 1000 次/天），存 GitHub Secrets，YAML 里 `env: LL_TOKEN: ${{ secrets.LL_TOKEN }}` |
| 2 | **LL API 对中国事件覆盖不足**（低轨组网批次常缺载荷细节，更新滞后数小时到数天） | 高/中 | 架构上已规避：中国事件走 overrides 完全人工、脚本绝不自动改写；Actions 只产出"疑义清单"提醒核实；LL 仅作国际数据源。另可补一个可选的「航天快讯 RSS 聚合」到变更报告（新华社/央视军事/我们的太空公众号需人工，不做自动抓取——中文源反爬与准确性风险高） |
| 3 | **GitHub 访问网络问题**（用户在中国大陆，Actions/推送/线上访问可能不稳；已确认有 Clash 代理） | 中/高 | ① 推送时：Clash 开 TUN/系统代理模式即可，`git config --global http.proxy http://127.0.0.1:7890`（Clash 默认端口）按需设置；② 线上访问：`github.io` 域名在大陆时通时断，**对策是接受"主用本地双击 + Pages 作为对外分享渠道"的双轨定位**；若分享对象均在国内且反馈打不开，备选方案：Gitee Pages 镜像（导入仓库，免费，但需实名+内容审核）或 Cloudflare Pages（绑定自定义域名，大陆可达性更好，操作复杂度+1，列为 B 计划）；③ Actions 排障无需代理（跑在 GitHub 服务器上） |
| 4 | **数据冲突：自动抓取 vs 手工补录** | 中/高 | 三层防线：① 合并规则白纸黑字——中国事件 overrides 独占写权，国际事件人工可 `locked:1` 锁定；② 每次自动 commit 前 validate-data.js 硬校验，不过则中止；③ git 版本管理兜底——任何一次自动更新改坏了，`git revert` 一条命令回滚到上一个好版本（这是阶段 0 最先做 git 的原因） |
| 5 | **自动更新覆盖 LYON 正在编辑的文件**（本地改了没 push，云端 bot 先 push 了） | 低/中 | 规范：先 push 自己的修改再等 bot 运行（bot 每天 09:00 跑一次，错开即可）；冲突万一发生，git pull 会提示，保留双方（merge）而非覆盖，冲突段以 overrides 机制天然规避 |
| 6 | **LL2 数据本身有错**（日期改动/状态误标） | 中/低 | source 字段保留 LL 详情页链接，发现可疑时人工点开核实；关键节点（失利/首飞/重大工程）在 overrides 锁定人工维护 |
| 7 | **浏览器兼容**（color-mix 需 Chrome/Edge 111+） | 低/低 | 维持 PRD 约定不做降级；页脚加一句"建议使用新版 Chrome/Edge 浏览器"提示即可 |
| 8 | **LYON 中断维护**（时间投入不可持续） | 中/中 | 设计已把人工量压到最低：日常仅看报告 5 分钟/天；中国事件每周集中录一次即可；全部操作手册沉淀在 docs/维护手册.md，任何中断后可凭文档无损恢复 |

---

## 附：本文档与任务的对应关系速查

| PRD backlog | 任务 | 阶段 |
|---|---|---|
| B1 数据自动更新 | T6/T7/T8 | 阶段 3 |
| B2 公网部署 | T5 | 阶段 2 |
| B3 10-12月回填 | T9 | 阶段 3 |
| B4 国际扩展 | T10 | 阶段 3 |
| B5 在轨动态 | T12 | 阶段 4 |
| B6 搜索 | T13 | 阶段 4 |
| B7 年份切换 | T14 | 延后 |
| 火箭规格增强（新） | T11 | 阶段 4 |
| 数据可追溯（新） | T3/T4 | 阶段 1 |
